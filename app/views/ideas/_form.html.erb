<%= form_for(@idea) do |f| %>

<% if @idea.errors.any? %>
  <h2><%= pluralize(@idea.errors.count, "error") %> prohibited this record from being saved:</h2>
  <ul>
    <% @idea.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.collection_select(:category_id, @categories.order(:name),:id, :name) %><br />

  <% @images.each do |image| -%>
<div>
  <%= check_box_tag(:image_ids, image.id, false, :name => 'idea[image_ids][]', id: "image-#{image.id}") %>
  <%= image.name %>
</div>
<% end %>


  <%= f.submit %>
<% end %>
